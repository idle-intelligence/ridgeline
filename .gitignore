# Rust
/core/target
**/*.rs.bk

# wasm-pack output (built artifact, regenerated)
/core/pkg
/web/pkg

# JS dev deps and test artifacts
/web/node_modules
/web/package-lock.json
/web/test-screenshot.png
/web/test-dive.png
/web/test-url-*.png
/web/ve-*.png
/web/ve-capture.mjs

# Python
__pycache__/
*.pyc
.venv/
venv/

# SRTM raw download cache (we commit the baked heightfield, not raw tiles)
/data/bake/cache/
/data/srtm-cache/

# Agent-dashboard worktree metadata
/.claude/

# OS
.DS_Store

# Internal working notes (AI session handoffs, plans) — not part of the public repo
/docs/HANDOFF.md
/docs/overnight-plan.md
